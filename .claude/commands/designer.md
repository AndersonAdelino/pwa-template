---
description: Phase 2 — invokes the designer subagent to convert the wireframe into a Hi-Fi design.
argument-hint: [preferências de estilo: cores, tipografia, mood — opcional]
---

Invoke the `designer` subagent via the Agent tool. Pass `$ARGUMENTS` as style preferences if provided.

The subagent owns Phase 2: propose design system, get approval, generate `designs/design.html`, update `PROJECT.md`.
